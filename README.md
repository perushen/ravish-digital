# ravish.digital

Static site for Ravish Digital Studio.

- `index.html` — the page (~82 KB)
- `assets/` — images, fonts, video, and JS, served as individual files
- `vercel.json` — long-lived caching for `assets/`, no-cache for the HTML

Deployed on Vercel. DNS at Cloudflare.
